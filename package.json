{
  "name": "bizpilot",
  "version": "0.1.0",
  "private": true,
  "description": "AI business assistant for small businesses — sales, inventory, invoicing, expenses and insights.",
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/api & npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down",
    "infra:reset": "docker compose down -v && docker compose up -d",
    "db:migrate": "npm run db:migrate --workspace=apps/api",
    "db:seed": "npm run db:seed --workspace=apps/api",
    "db:studio": "npm run db:studio --workspace=apps/api",
    "docs:docx": "node scripts/docs-to-docx.mjs",
    "momo:provision": "node scripts/momo-provision.mjs",
    "momo:test-payment": "node scripts/momo-test-payment.mjs",
    "momo:diagnose": "node scripts/momo-diagnose.mjs",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "docx": "^9.7.1"
  }
}
